#include <bits/stdc++.h>

int main() {
    int n, p, d;
    std::string pattern;

    std::cin >> n >> p >> d;
    std::cin >> pattern;

    pattern += pattern;

    for (int i = 0; i < pattern.size(); i++) {
        int from;
        int to;
    }

    return 0;
}
