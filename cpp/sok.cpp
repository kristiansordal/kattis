#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, c, i, j, k;
    cin >> a >> b >> c >> i >> j >> k;

    cout << a % i << " " << b % j << " " << c % k << endl;

    return 0;
}
